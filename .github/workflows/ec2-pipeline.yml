name: ec2-pipeline
on: workflow_dispatch

jobs:
  build-jobs:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: get pem file
        run: |
          echo "${{ secrets.secretkey }}" > key.pem
          chmod 400 key.pem

      - name: copy files
        run: |
          scp -o StrictHostKeyChecking=no -i key.pem app.py ec2-user@YOUR-IP:/home/ec2-user/
          scp -o StrictHostKeyChecking=no -i key.pem requirements.txt ec2-user@YOUR-IP:/home/ec2-user/

      - name: run application
        run: |
          ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@YOUR-IP "
          cd /home/ec2-user &&
          python3 -m venv virtualenv &&
          source virtualenv/bin/activate &&
          pip3 install -r requirements.txt &&
          python3 app.py
          "
